# Local design and assistant development only. Never commit real credentials.
AI_GATEWAY_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# Optional LOCAL advisory judge. Existing workspaces retain their review policy.
# Set both to opt in; no environment setting grants Human Approval.
# PROOFPRESS_JUDGE_PROVIDER=typesafe
# PROOFPRESS_JUDGE_MODEL=jev-latest
TYPESAFE_API_KEY=
# Hosted: select TypeSafe in Admin and save a workspace-specific provider key.
# Hosted intentionally does not fall back to this local TypeSafe key.
