# Local design and assistant development only. Never commit real credentials.
AI_GATEWAY_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# Optional LOCAL advisory judge. Existing workspaces retain their review policy.
# Install the Node bridge dependencies before using Vercel Jev.
# PROOFPRESS_JUDGE_PROVIDER=vercel_jev
# PROOFPRESS_JUDGE_MODEL=typesafe-ai/jev
# Hosted: select Vercel Jev in Admin and save a workspace-specific Gateway key.
# Hosted intentionally does not fall back to the local AI_GATEWAY_API_KEY.

# Alternative: invite-only TypeSafe direct API (no Node bridge required).
# PROOFPRESS_JUDGE_PROVIDER=typesafe
# PROOFPRESS_JUDGE_MODEL=jev-latest
TYPESAFE_API_KEY=
